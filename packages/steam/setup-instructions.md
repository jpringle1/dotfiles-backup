i need to symlink these files to their destinations and make sure steam cannot delete them.
This will only apply settings *before* a user has been logged it, so make sure this happens before running steam for the first time.
I don't think this will set 24hour clock or default page setting (but it may work with sharedconfig).

